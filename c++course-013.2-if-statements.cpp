#include <iostream>
using namespace std;

int main()
{
    
    bool isMale = true;
    bool isTall = true;

    if(isMale && isTall) // Vamos especificar uma condição - && and operator
    {   
        cout << "You're a tall male.";
    }

    else
    { 
        "You aren't a male, you're a female and aren't tall.";
    }

    return 0;
}
